export interface ChartDataArc {
  //TODO - rename it to something more meaningful
  y: number[];
  x: number[];
  yLabel?: string;
  xLabel?: string;
}

export interface Point {
  x: number;
  y: number;
}
